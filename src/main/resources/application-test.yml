fint:
 events:
  test-mode: true
  queue-endpoint-enabled: true
  orgIds: 'mock.no'

 audit:
  test-mode: true

 relations:
  force-https: false

 consumer:
  cache:
   initialDelay:
    organisasjonselement: 1000

link-mapper-base-url: http://localhost:8080